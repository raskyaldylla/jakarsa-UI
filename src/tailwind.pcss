:root {
  --color-primary-light: #59B2F4;
  --color-primary-dark: #127FCF;
  --color-secondary: #061429;
}

@tailwind base;
@tailwind components;
@tailwind utilities;

h1 {
  @apply text-2xl;
}

button, a, svg, path, .hoverable, {
  @apply transition-all duration-150 ease-in-out;
} 